@model IEnumerable<FrituurzaakV5.Models.Order>

@{
    ViewData["Title"] = "Index";
}

<body style="background-color: black; color: white">


    <div class="card border-info mb-4 bg-black text-center mx-auto" style="max-width: 50rem;">
        <div class="card-body">
            <h3 class="card-title">Gebruik de code hieronder om je bestelling af te halen</h3>
            <p class="card-text">
                <h1 style=font-size:200px>
                    <p id="demo"></p>
                </h1>


            </p>
        </div>

        <script> //Generieke getal onder de nul voor afhaalcode
            document.getElementById("demo").innerHTML =
                Math.floor(Math.random() * 100);
        </script>




        <button class="btn btn-info" onclick="window.location.href = '..';">
            <h2>Bedankt voor je bestelling</h2>
        </button>
</body>

@*<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderregelId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderregelId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
*@